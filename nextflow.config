// Nextflow flags
nextflow.enable.moduleBinaries = true

// Unscoped options
cleanup   = false
workDir   = "nf-work"

// Global default params, used in configs
params {
  // pipeline parameters
  help     = false
  datasets = null
  mapper   = null
  wl_basedir = "${projectDir}/data/whitelists/"

  // output parameters
  publish_mode = 'copy'
  output_dir = "results"
}

// Singularity environment parameters
singularity {
  enabled     = true
  autoMounts  = true
  cacheDir    = '/nfs/cellgeni/singularity/images/'
  runOptions  = '-B /lustre,/nfs'
  pullTimeout = '100m'
}

// Configuring LSF job submission parameters for each process
executor {
  name           = 'lsf'
  perJobMemLimit = true
}

// Configure process parameters
process {
    errorStrategy = 'retry'
    maxRetries    = 5
}

// Load module configurations
includeConfig 'modules/local/reprocess10x/mappingqc/module.config'
includeConfig 'modules/local/reprocess10x/parsemetadata/module.config'
includeConfig 'modules/local/irods/storefile/module.config'
includeConfig 'modules/local/irods/attachcollectionmeta/module.config'

// Capture exit codes from upstream processes when piping
process.shell = ['/bin/bash', '-euo', 'pipefail']

manifest {
    name            = 'cellgeni/nf-reprocessing-public-10x'
    author          = "CellgenI Team"
    homePage        = 'https://github.com/cellgeni/nf-reprocessing-public-10x'
    description     = "Pipeline to load and process public 10x datasets"
    mainScript      = 'main.nf'
    nextflowVersion = '!>=24.04.2'
    version         = '23-263'
}
