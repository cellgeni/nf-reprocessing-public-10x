process {
    withName: '.*SRA2FASTQ' {
        queue = 'long'
        cpus = 16
        memory     = { 128.GB + 64.GB * (task.attempt - 1) }
        errorStrategy = { task.attempt == 5 ? 'ignore' : 'retry' }
        maxRetries    = 5
    }
}