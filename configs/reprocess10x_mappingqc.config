process {
    withName: '.*REPROCESS10X_MAPPINGQC' {
        queue          = 'normal'
        cpus           = 1
        memory     = { 2.GB + 4.GB * (task.attempt - 1) }
        // publishDir     = [
        //     path: { "${params.outdir}/starsolo/${meta.id}" },
        //     mode: params.publish_mode,
        //     pattern: "*.tsv"
        // ]
    }
}