process {
    withName: '.*CELLRANGER_COUNT.*' {
        ext.args = "--chemistry=auto --create-bam=false"
        queue = 'long'
        cpus = 16
        memory     = { 128.GB + 64.GB * (task.attempt - 1) }
        maxRetries    = 5
        //clusterOptions = '-sp 51'
    }
}