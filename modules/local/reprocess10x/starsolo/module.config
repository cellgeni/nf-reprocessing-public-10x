process {
    withName: '.*REPROCESS10X_STARSOLO.*' {
        queue = 'long'
        cpus = 16
        memory = '128 GB'
        publishDir = [
            path: { "${params.output_dir}/starsolo/${meta.dataset_id}/${meta.sample_id}" },
            mode: params.publish_mode,
        ]
    }
}