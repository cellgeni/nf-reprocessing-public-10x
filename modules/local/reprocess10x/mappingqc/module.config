process {
    withName: '.*REPROCESS10X_MAPPINGQC' {
        queue = 'normal'
        cpus  = 1
        memory = '2 GB'
        publishDir = [
            path: { "${params.output_dir}/starsolo/${meta.id}" },
            mode: params.publish_mode,
            pattern: "*.tsv"
        ]
    }
}