process {
    withName: *REPROCESS10X_PARSEMETADATA {
        queue = 'transfer'
        cpus = 1
        memory = '4 GB'
        array = 10
        publishDir = [
            path: { "${params.output_dir}/metadata/${meta.id}" },
            mode: params.publish_mode,
        ]
    }
}