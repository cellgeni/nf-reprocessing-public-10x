process {
    withName: '.*REPROCESS10X_SRA2FASTQ' {
        queue = 'long'
        cpus = 16
        memory = '128 GB'
        publishDir = [
            path: { "${params.output_dir}/raw/${meta.dataset_id}/${meta.sample_id}" },
            mode: params.publish_mode,
        ]
    }
}