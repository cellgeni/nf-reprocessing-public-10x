#!/bin/bash

set -euo pipefail

samplefile="actions/sample.list"

[[ -e "$samplefile" ]] || (echo "File "$samplefile" not found" && false)

nextflow run cellgeni/nf-reprocessing-public-10x \
  --samplefile $samplefile \
  -w work \
  --ansi-log false \
  -resume
